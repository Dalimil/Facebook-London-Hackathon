{
	"manifest_version": 2,

	"name": "M",
	"description": "This extension allows you to have multiple browser tabs easily visible.",
	"version": "0.1",

	"permissions": [
		"webview"
	],

	"app": {
		"background": { /* Run in the background */
			"scripts": ["jquery.js", "background.js"]
		}
	}
}
